include head 
include navbar 
body 
    != messages('message', locals)
    if errors 
        each error in errors 
          div(class="alert alert-danger") #{error.msg}
    block content
include footer 
